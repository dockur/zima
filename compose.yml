services:
  zima:
    image: dockurr/zima
    container_name: zima
    devices:
      - /dev/kvm
      - /dev/net/tun
    cap_add:
      - NET_ADMIN
    ports:
      - 8080:80
    volumes:
      - ./zima:/storage
    restart: always
    stop_grace_period: 2m
