services:
  zima:
    image: dockurr/zima
    container_name: zima
    devices:
      - /dev/kvm
      - /dev/net/tun
    cap_add:
      - NET_ADMIN
    ports:
      - 8006:8006
      - 8080:8080
    volumes:
      - ./zima:/storage
    restart: always
    stop_grace_period: 2m
